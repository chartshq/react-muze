import muze from '@chartshq/muze';
import { Headers } from './headers';
import { POSITION, ALIGNMENT } from './constants';

const { html } = muze.Operators;

export { Headers, POSITION, ALIGNMENT, html };
